server:
  port: 8000

spring:
  application:
    name: consul-config
  profiles:
    active: native
  cloud:
    consul:
      host: 10.10.35.249
      port: 8500
      discovery:
        service-name: "${spring.application.name}"
        prefer-ip-address: true
        hostname: 10.10.35.236
    config:
      server:
        native:
          search-locations: classpath:/config
        bootstrap: true